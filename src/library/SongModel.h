#pragma once

#include <QObject>

// Lightweight song data struct (for passing between QML and C++)
// Full definition is in LibraryManager.h but SongModel.h is for lightweight includes
#include "LibraryManager.h"
